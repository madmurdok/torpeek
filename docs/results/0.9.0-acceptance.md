# Acceptance run, torpeek 0.9.0

Run 2026-09-04T19:20:26+03:00.

Machine while measuring: `19:20  up 1 day,  7:13, 7 users, load averages: 4.02 4.47 6.63`. Elapsed times are only
comparable against another run on a similarly loaded machine; traffic is
comparable regardless.

Torrents:

- live: https://archive.org/download/Sintel/Sintel_archive.torrent (file sintel-2048-stereo.mp4)

| # | criterion | verdict |
| --- | --- | --- |
| 1 | min-time: 20 frames within 2 minutes and 150 MB | **met** |
| 2 | min-traffic: the same 20 frames within 60 MB | **met** |
| 3 | a repeat run with the same parameters makes no network request | **met** |
| 4 | cancelling keeps frames; the next run fills the gaps | **met** |
| 5 | a private torrent makes no DHT or PEX request | **met** |
| 6 | a multi-file torrent yields a frame set and manifest per video file | **met** |
| 7 | with pieces unavailable the set is still complete and shifts are marked | **met** |

## 1. min-time: 20 frames within 2 minutes and 150 MB

**met** — measured against one file of a public torrent; the criterion names a ~10 GB film, and the largest file here is smaller than that

| measurement | value |
| --- | --- |
| frames | 20 of 20 |
| downloaded | 101.6 MiB (ceiling 150.0 MiB) |
| elapsed | 61.1s |
| outcome | completed |

## 2. min-traffic: the same 20 frames within 60 MB

**met** — measured against one file of a public torrent; the criterion names a ~10 GB film, and the largest file here is smaller than that

| measurement | value |
| --- | --- |
| frames | 20 of 20 |
| downloaded | 46.0 MiB (ceiling 60.0 MiB) |
| elapsed | 33.5s |
| outcome | completed |

## 3. a repeat run with the same parameters makes no network request

**met**

| measurement | value |
| --- | --- |
| first run | 4 frames, 19.3 MiB |
| rerun | 4 frames, 0.0 MiB |
| rerun elapsed | 0.0s |

## 4. cancelling keeps frames; the next run fills the gaps

**met**

| measurement | value |
| --- | --- |
| cancelled after | 2 frames, outcome cancelled |
| kept on disk | 2 |
| second run | 6 of 6 frames, 13.3 MiB |
| reused untouched | 2 of 2 |

## 5. a private torrent makes no DHT or PEX request

**met** — tested against a private torrent built for the run: a public torrent cannot carry the private flag and still be public, so this condition cannot be ordered from a live swarm

| measurement | value |
| --- | --- |
| private flag read offline | true |
| DHT requested by config | true |
| DHT actually running | false |

## 6. a multi-file torrent yields a frame set and manifest per video file

**met** — three frames per file rather than twenty: the criterion is a set per file, and twenty of each would cost a gigabyte to prove the same thing

| measurement | value |
| --- | --- |
| files processed | 6 |
| manifests written | 6 |
| frames | 18 |
| downloaded | 63.3 MiB |

## 7. with pieces unavailable the set is still complete and shifts are marked

**met** — tested against a seeder whose copy is deliberately wrong across the middle of the file, so those pieces fail its own verification and it never offers them; a healthy public swarm has no hole to order

| measurement | value |
| --- | --- |
| frames | 3 of 3 |
| marked shifted in the manifest | 1 |
| downloaded | 2.3 MiB |

