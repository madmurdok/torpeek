// Package version carries the build identity of the binary. It is bumped on a
// release branch so that any produced manifest can name the code that made it.
package version

// Version is the release this binary claims to be.
const Version = "0.9.0"
