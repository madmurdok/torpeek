// Package web serves the embedded frontend on localhost and streams core
// events to it, so one binary is both the engine and the UI a person without
// a terminal can use.
//
// Requirements: section 3.3.
package web
