// Package tui renders a live terminal view of a run - piece map, peers,
// timeline with capture points, spent time and traffic against the limits.
// It consumes the core event stream and holds no logic of its own.
//
// Requirements: section 3.2.
package tui
